name: Rust Lints 👨‍🔧

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.ref }}-rust-lints
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  packages: write
  pages: write
  id-token: write
  contents: read

jobs:
  lints:
    name: ${{ matrix.command.step }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        command:
          - step: Format
            name: Run format
            cmd: cargo fmt -- --check
          - step: Clippy
            name: Run clippy
            cmd: cargo clippy -- -Dwarnings
          - step: Tests
            name: Run tests
            cmd: cargo clippy --tests -- -Dwarnings

    steps:
      - uses: actions/checkout@v6
        with:
          submodules: "recursive"
      - uses: rui314/setup-mold@v1
      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt
          cache: true
      - name: Install foundry
        uses: foundry-rs/foundry-toolchain@v1
      - name: ${{ matrix.command.name }}
        run: ${{ matrix.command.cmd }}