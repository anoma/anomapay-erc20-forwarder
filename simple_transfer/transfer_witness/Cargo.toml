[package]
name = "transfer_witness"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = "1.0"
arm = { git = "https://github.com/anoma/arm-risc0", branch = "develop", default-features = false }
arm-gadgets = { git = "https://github.com/anoma/arm-risc0", branch = "develop" }
rand = "0.8"
alloy-primitives = "1.0.23"
alloy-sol-types = "1.0.23"
k256 = { version = "=0.13.3", features = ["arithmetic", "serde", "expose-field", "std", "ecdsa", "hash2curve"], default-features = false }

[dev-dependencies]
hex = "0.4"